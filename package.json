{
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build --configuration=production --optimization=true --build-optimizer=true --aot=true --source-map=false",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  }
}